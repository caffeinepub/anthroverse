{
  "frontend/src/pages/FeedPage.tsx": "Community feed page with pending approvals (admin only), create-post toggle, PostFilters, and inline post list — removes the non-existent `useSearchPostsByMember` import and replaces it with client-side filtering of the already-fetched posts array.",
  "frontend/src/pages/EventsPage.tsx": "Events page with upcoming/pending tabs, event cards, create event form, and registration management — fixes `useGetEventRegistrations` call to pass `null` instead of `undefined` when no event is selected, matching the `bigint | null` signature.",
  "frontend/src/pages/ChapterGrowthPage.tsx": "Chapter Growth analytics page with stats grid, role distribution, and private group feeds — fixes `GroupFeed` prop from `title` to `groupName` to match the component's actual interface.",
  "frontend/src/App.tsx": "Root app component with routing, auth phases, and query client — adds the missing `/chapter-growth` route so `ChapterGrowthPage` is reachable and the path is a valid registered route for TypeScript navigation.",
  "frontend/src/components/auth/SignupForm.tsx": "Multi-step signup form that collects name, email, and optional profile picture — fixes the User object passed to saveProfile to include the required `companyName: ''` and `description: ''` fields.",
  "frontend/src/pages/ProfileSetupPage.tsx": "Standalone profile setup page shown after first login — fixes the User object passed to saveProfile to include the required `companyName: ''` and `description: ''` fields.",
  "frontend/src/lib/utils.ts": "Core utility functions including `cn`, `roleToLabel`/`getRoleLabel`, `roleBadgeClass`, `categoryToLabel`, `formatTimestamp`, `getInitials`, `canApproveContent`, `postStatusLabel`, plus `ROOT_ADMIN_EMAIL` constant and `isRootAdminEmail` helper (with `isRootAdmin` alias) for case-insensitive root admin email checks.",
  "frontend/src/components/AppSidebar.tsx": "App sidebar with brand header, navigation links (Feed at `/`, Events, Chapter Growth), optional admin section, and footer with profile/logout — fixes the `/feed` route to `/` to match the registered root route.",
  "frontend/src/utils/permissions.ts": "Permission utility module with all role-based check functions — ensures `canManageUsers` correctly returns true for all executive roles (president, vicePresident, secretaryTreasurer, rootAdmin) so approve/reject buttons are enabled for those users.",
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for backend interactions — ensures `useApproveOrRejectUser` mutation has proper error handling with toast notifications, and `useGetPendingUsers` uses the dedicated `getPendingUsers` backend endpoint with retry logic.",
  "frontend/src/pages/AdminPage.tsx": "Admin panel with Pending Approvals, All Members, and Assign Roles tabs — fixes the approval flow by properly waiting for auth/profile to load before rendering (shows skeleton during load, access-denied if insufficient role), and wires approve/reject buttons directly to the `useApproveOrRejectUser` mutation with per-row loading indicators and toast feedback.",
  "frontend/src/pages/LoginPage.tsx": "Standalone self-contained login page with background image, overlay, logo, welcome text, and Internet Identity login button — no AuthLayout dependency, renders unconditionally without any state-gated logic that could cause blank screens.",
  "frontend/src/components/PostCard.tsx": "Legacy PostCard component that renders a post with likes, comments, approve/delete actions — fixes `canApproveContent` call to use the single-argument signature `canApproveContent(userRole) || isAdmin` instead of the removed two-argument form.",
  "frontend/src/components/feed/CreatePostForm.tsx": "Post creation form with category selector, image upload, and content textarea — fixes the `useSubmitPost` call to use `imageBlob` instead of `image` to match the updated hook signature.",
  "frontend/src/components/feed/PostCard.tsx": "Feed-specific PostCard component — fixed `useGetComments` call to pass `undefined` instead of `null` when collapsed, and replaced the missing `canApproveContent` import with the correct `canApprovePost` from permissions utils.",
  "frontend/src/components/layout/MainLayout.tsx": "App shell with SidebarProvider, sticky header (h-14, z-30, shrink-0), scrollable main content via Outlet — uses a clean flex column layout inside SidebarInset with no overlapping elements; header is sticky within the scroll container.",
  "frontend/src/components/ErrorBoundary.tsx": "React class-based error boundary that catches render errors anywhere in the component tree and displays a user-friendly recovery UI with a refresh button instead of a blank page.",
  "frontend/src/main.tsx": "Application entry point that sets up a single QueryClientProvider, InternetIdentityProvider, ThemeProvider, and wraps the entire App in the new ErrorBoundary component to catch any uncaught render errors.",
  "frontend/src/components/ProfileSetupModal.tsx": "Standalone profile setup screen shown on first login — collects name and email, calls registerUser, detects the root admin email for a special welcome message, then invalidates and refetches the profile query so App.tsx transitions to the correct phase.",
  "frontend/src/components/WaitingForApproval.tsx": "Waiting for approval screen with a \"Check Again\" refresh button and a prominent \"Log Out\" button that calls clear() and clears the query cache.",
  "frontend/src/components/feed/PendingApprovals.tsx": "Pending post approvals component — fixes `useGetPosts(null)` to `useGetPosts(undefined)` to match the updated hook signature.",
  "frontend/src/components/events/CreateEventForm.tsx": "Event creation form — converts banner file to `ExternalBlob` and passes `bigint | null` for registration limit, matching the updated `useCreateEvent` hook signature.",
  "frontend/src/pages/ProfilePage.tsx": "User profile page with view and edit modes — uses `max-w-2xl mx-auto px-4 py-6 space-y-6` layout with no positioning conflicts.",
  "frontend/src/pages/AuthPage.tsx": "Auth page that renders `LoginPage` for the login view and a self-contained `SignupForm` (no props) for the signup view, with a toggle link between the two.",
  "frontend/src/components/admin/RoleAssignmentPanel.tsx": "Admin panel component for assigning roles to approved members — fixes the `useAssignRole` call to use `userPrincipal` instead of `user` to match the updated hook signature.",
  "frontend/src/components/chapter-growth/GroupFeed.tsx": "Private group feed component that renders posts for a specific category — updated to fetch `userProfile` and `identity` internally and pass the required `userRole`, `callerPrincipal`, `onLike`, `onApprove`, and `onDelete` props to the `PostCard` component.",
  "frontend/src/pages/ChapterMeetingPage.tsx": "Chapter Meetings page showing upcoming events and meeting notes — uses `max-w-3xl mx-auto px-4 py-6 space-y-6` layout with no positioning conflicts."
}